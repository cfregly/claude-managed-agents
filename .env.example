# Copy to .env or export in your shell. .env is git-ignored.
# Required: every run calls the real Managed Agents API and fails fast without this key.
# The Managed Agents beta (managed-agents-2026-04-01) must also be enabled on your org.
ANTHROPIC_API_KEY=
