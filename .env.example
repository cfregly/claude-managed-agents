# Copy to .env or export in your shell. .env is git-ignored.
# Without a key, `python run.py` runs the whole surface tour as an offline dry run.
# --live also needs the Managed Agents beta enabled on your org.
ANTHROPIC_API_KEY=
